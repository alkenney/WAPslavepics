domain = 'http://walshbr.com/'
pages = ['about','blog','pedagogy','projects','cv']
domain'pages[0],pages[1],pages[2],pages[3],pages[4]'.split(domain',')
    print(domain)

urls = []
for page in pages:
        url = domain + page
        urls.append(url)
